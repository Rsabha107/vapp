<a href="javascript:void(0)" data-table="{{ $table }}" id="{{ $selectionId }}" data-id="{{ $dataId }}">
    <button class="{{ $buttonClass }}" data-bs-toggle="tooltip" data-bs-placement="right"
        data-bs-original-title="{{ $title }}">
        <span class="fa-solid fa-rotate-right me-sm-2"></span><span class="d-none d-sm-inline">{{ $title }}</span>
    </button>
</a>
